export type Point = {
  readonly x: number;
  readonly y: number;
};

export type Rectangle = {
  readonly left: number;
  readonly right: number;
  readonly bottom: number;
  readonly top: number;
};
